# کارهای باقی‌مانده — پول‌بین

هر چیزی که در طول توسعه آگاهانه به تعویق انداختیم، با دلیلش. راهنمای اجرا و
عیب‌یابی محیط در [RUN.md](RUN.md) است، اینجا فقط کارهای کد و محصول.

آخرین به‌روزرسانی: ۱۷ شهریور ۱۴۰۵

---

## ۱. مسدودکننده‌های انتشار

این‌ها باید **قبل از** فرستادن به کافه‌بازار حل شوند.

### ~~معماری بیلد را برگردان~~ ✅ انجام شد
`reactNativeArchitectures` در [android/gradle.properties](android/gradle.properties) دوباره
روی هر چهار معماری است، پس APK روی گوشی‌های واقعی ARM نصب می‌شود.

### کلید امضای release بساز
[android/app/build.gradle](android/app/build.gradle) هنوز نسخه‌ی release را با
**کلید debug** امضا می‌کند. برای نصب روی گوشی خودت کافی است، ولی کافه‌بازار قبولش نمی‌کند
و دو APK با کلیدهای متفاوت روی هم آپدیت نمی‌شوند.

کلید را بساز و رمزش را در `~/.gradle/gradle.properties` بگذار — **نه** داخل مخزن گیت.

### احراز هویت واقعی
صفحه‌ی ورود جعلی حذف شد و جایش [ProfileContext](src/state/ProfileContext.tsx) نشست که
فقط یک نام محلی نگه می‌دارد. تا وقتی همه‌چیز روی گوشی است این درست‌ترین حالت است، ولی
با آمدن سرور باید ورود واقعی (پیامک + توکن) برگردد و آن‌وقت:

- توکن **نباید** در MMKV رمزنگاری‌نشده بنشیند. `react-native-keychain` از Keystore
  خود اندروید استفاده می‌کند و برای اپ مالی حداقلِ لازم است.
- `user_id` همیشه باید از توکن سمت سرور بیرون کشیده شود، نه از بدنه‌ی درخواست.

سرور در `polbin_server` نوشته و تست شده ولی عمداً وصل نشده است.

### حجم APK — چه شد و چه چیزی باقی است

APK اول ۶۹ مگابایت بود. با دو تغییر در
[android/app/build.gradle](android/app/build.gradle) به **۲۲ مگابایت** رسید:

- **تفکیک بر اساس معماری** (`splits.abi`) — قبلاً یک فایل هر چهار معماری را با خود
  می‌برد در حالی که هر گوشی فقط یکی را اجرا می‌کند. ۵۸ مگابایت کتابخانه‌ی نیتیو
  بود که سه‌چهارمش روی هر دستگاه بی‌مصرف می‌ماند.
- **روشن کردن R8** (`enableProguardInReleaseBuilds = true`) — `classes.dex` را
  کوچک می‌کند. تست شد: ماژول نیتیو، پشتیبان، بازگردانی و گزارش PDF همه سالم‌اند.

کف حجمی که باقی مانده تقریباً تمامش موتور است، نه کد ما:

```
libreactnative.so   6.9MB
libhermesvm.so      2.5MB
libc++_shared.so    1.3MB
سایر .so            ~4MB
classes.dex         ~5MB
کد و فونت خودمان    ~3MB   ← بسته‌ی JS ۱.۴ + وزیرمتن ۰.۵ + منابع
```

پایین‌تر از این با ری‌اکت‌نیتیو ممکن نیست. اگر روزی حجم بحرانی شد، تنها گزینه‌ی
واقعی بازنویسی نیتیو (Kotlin) است که برای MVP توجیه ندارد.

اگر کافه‌بازار آپلود چند APK را قبول کند، هر چهار فایل را بده تا هر کاربر
سبک‌ترین نسخه را بگیرد؛ وگرنه فقط `arm64-v8a` را بفرست که اکثریت مطلق
گوشی‌های امروزی است.

### پاک‌سازی مانیفست
مجوزهای اضافه‌ی `AndroidManifest.xml` را حذف کن و روی اندروید ۸ به بالا تست کن
(`minSdkVersion` روی ۲۴ است).

### تست روی گوشی واقعی
تمام تست‌های تا امروز روی شبیه‌ساز `x86_64` بوده. اپ **هرگز روی یک گوشی ARM اجرا نشده**.
برای اپ فارسی ریسک واقعی دارد: رندر فونت، RTL، و رفتار منوی هم‌رسانی روی پوسته‌های
سازنده‌ها (شیائومی، سامسونگ) با اندروید خام فرق می‌کند.

---

## ۲. قابلیت‌های اصلی که هنوز ساخته نشده‌اند

### ~~Share Intent اندروید~~ ✅ انجام شد
هر دو مسیر پیاده و تست شدند: اجرای سرد (از `getLaunchOptions` به‌عنوان initialProps) و
اپِ باز (`onNewIntent` → رویداد `polbin.sharedText`). کد در
[MainActivity.kt](android/app/src/main/java/com/polbin/MainActivity.kt) و
[shareIntent.ts](src/services/shareIntent.ts).

دو کاستی باقی‌مانده:

- **اگر پروسه زنده ولی ReactContext نابود شده باشد**، `onNewIntent` جایی برای فرستادن
  رویداد ندارد و متن بی‌صدا از دست می‌رود. حالت نادری است (اندروید معمولاً کل پروسه را
  می‌کشد)، ولی راه درست ذخیره‌ی متن در MMKV و خواندنش موقع بالا آمدن context است.
- **فقط `ACTION_SEND` با `text/plain`** پشتیبانی می‌شود. `ACTION_SEND_MULTIPLE`
  (هم‌رسانی چند پیامک با هم) نادیده گرفته می‌شود.

### بک‌اند — ساخته شد ولی وصل نیست
سرور در `polbin_server/` نوشته و تست شده است: احراز هویت با کد پیامکی، توکن JWT،
جدول‌های PostgreSQL و مسیرهای تراکنش و دسته‌بندی با جداسازی بر اساس `user_id`.
۲۲ تست سرتاسری پاس می‌شود، از جمله اینکه کاربری نتواند داده‌ی کاربر دیگر را بخواند یا پاک کند.

**عمداً به اپ وصل نشده.** برای MVP تصمیم گرفتیم همه‌چیز روی دستگاه بماند: بک‌اند برای
کاربر روز اول ارزشی ندارد، ورود اجباری بزرگ‌ترین نقطه‌ی ریزش است، هزینه‌ی هاست و پیامک
دارد، و داستان «پیامک از گوشی خارج نمی‌شود» را از بین می‌برد.

سیگنال‌هایی که یعنی وقتش رسیده: کاربرها بپرسند «گوشی‌ام را عوض کردم، داده‌هایم کجاست؟»،
یا قابلیتی بخواهی که ذاتاً سرور می‌خواهد.

وقتی وصلش کردیم، سرور باید **منبع حقیقت نباشد** — معماری local-first بماند و سرور فقط
همگام‌سازی و پشتیبان باشد، تا اپ آفلاین کامل کار کند.

مسیر وصل کردن، جایگزینی بدنه‌ی این پنج تابع است:

| تابع | endpoint |
|---|---|
| `requestOtp` | `POST /auth/otp/request` |
| `verifyOtp` | `POST /auth/otp/verify` |
| `parseSmsRemote` | `POST /sms/parse` |
| `fetchTransactions` | `GET /transactions` |
| `createTransaction` | `POST /transactions` |

- [src/services/smsParser.ts](src/services/smsParser.ts) را به Node منتقل کن — تابع خالص
  است و هیچ وابستگی به ری‌اکت‌نیتیو ندارد.
- OTP واقعی از یک پنل پیامکی ایرانی.

### الگوی کش را عوض کن
[src/state/TransactionsContext.tsx](src/state/TransactionsContext.tsx) الان **یک بار** از
سرور seed می‌گیرد و بعد همیشه از کش می‌خواند. این عمدی بود تا داده‌ی نمونه تراکنش‌های
دستی کاربر را پاک نکند.

با بک‌اند واقعی باید بشود: «کش را فوری نشان بده، هم‌زمان از سرور به‌روز کن».

---

## ۳. باگ‌ها و کاستی‌های شناخته‌شده

### کارت‌به‌کارت شماره‌ی کارت اشتباه می‌گیرد
پارسر با `/\*{2,}\s*(\d{4})/` اولین شماره‌ی کارت را برمی‌دارد که در پیامک انتقال وجه
کارت **مقصد** است نه مبدأ. باید با کلیدواژه‌های «به:» و «از:» تفکیک شود.

### دسته‌های پیش‌فرض حذف نمی‌شوند
تصمیم عمدی: `MERCHANT_RULES` در پارسر مستقیم به شناسه‌های `food`، `transport` و
بقیه اشاره می‌کند. اگر کاربر بخواهد دسته‌ی پیش‌فرضی را نبیند، راه درست **مخفی کردن**
است نه حذف — هنوز پیاده نشده.

### مهاجرت کلیدهای ذخیره‌سازی
کلیدهای MMKV نسخه‌دارند (`transactions.v1`) که جلوی کرش با داده‌ی ناسازگار را می‌گیرد،
ولی منطق مهاجرت فقط برای یک مورد نوشته شده.

**نمونه‌ی واقعی که پیش آمد:** با افزودن درآمد، هر دسته فیلد `kind` گرفت. دسته‌های
دلخواهی که کاربر قبلاً ساخته بود این فیلد را نداشتند و `categoriesOfKind` بی‌صدا
حذفشان می‌کرد. مهاجرت در [CategoriesContext.tsx](src/state/CategoriesContext.tsx)
اضافه شد که مقدار `debit` را برایشان می‌گذارد.

هر تغییر ساختاری بعدی همین مراقبت را لازم دارد.

### MMKV برای مقیاس بزرگ ساخته نشده
کل آرایه‌ی تراکنش‌ها زیر **یک کلید** به‌صورت JSON ذخیره می‌شود، پس هر ثبت تراکنش
تمام لیست را دوباره سریالایز و کل فایل را بازنویسی می‌کند. روی ۲۰ تراکنش نامحسوس
است، روی چند هزار تا محسوس می‌شود.

دو نشانه که وقت مهاجرت به SQLite رسیده: کاربران چند صد تراکنش جمع کنند، یا بخواهی
جست‌وجو و گزارش ماه‌به‌ماه اضافه کنی که با آرایه‌ی JSON یعنی خواندن کل داده در حافظه.

### ~~هیچ پشتیبان یا خروجی‌ای وجود ندارد~~ — انجام شد

صفحه‌ی «پشتیبان و خروجی» ([src/screens/BackupScreen.tsx](src/screens/BackupScreen.tsx))
با ماژول نیتیو [PolbinFilesModule.kt](android/app/src/main/java/com/polbin/PolbinFilesModule.kt)
سه کار می‌کند: خروجی JSON، بازگردانی از فایل، و گزارش PDF از راه پنجره‌ی چاپ اندروید.

آنچه هنوز باقی است:

- **گزارش PDF بازه‌ی زمانی ندارد** — همیشه همه‌ی تراکنش‌ها را چاپ می‌کند. وقتی کاربر
  دو سال داده داشته باشد، گزارش «فروردین تا خرداد» لازم می‌شود.
- **بازگردانی جایگزین می‌کند، ادغام نمی‌کند** — عمدی است تا تراکنش تکراری ساخته نشود،
  ولی کاربری که روی دو گوشی داده دارد راهی برای یکی کردنشان ندارد.
- **فونت گزارش، فونت سیستم است** — وزیرمتن داخل WebView در دسترس نیست. برای یکدست
  شدن باید فونت را base64 در همان HTML جا داد؛ حجم PDF بیشتر می‌شود.
- **پشتیبان خودکار نیست** — کاربر باید یادش باشد دکمه را بزند. یادآور دوره‌ای یا
  ذخیره‌ی خودکار در پوشه‌ی دانلود قدم بعدی است.

---

### `presentation: 'modal'` برداشته شد — روی iOS باید بازبینی شود

اولین بیلد release نشان داد هر پنج صفحه‌ی فرعی با `presentation: 'modal'` اپ را می‌بندند:

```
IllegalStateException: addViewAt: failed to insert view [456] into parent [200] at index 2
  at com.swmansion.rnscreens.ScreenContainer.addScreen(ScreenContainer.kt:107)
Caused by: IndexOutOfBoundsException: Index: 2, Size: 1
```

آن حالت در `react-native-screens` یک ScreenStack جداگانه می‌سازد و با Fabric شمارش
فرزندهای نیتیو با درخت React هم‌خوان نمی‌ماند. **در بیلد debug هیچ‌وقت رخ نمی‌دهد** —
تمام صفحه‌ها ماه‌ها در debug باز می‌شدند و مشکلی نداشتند.

روی اندروید `slide_from_bottom` همان ظاهر را می‌دهد، پس چیزی از دست نرفت. ولی روی iOS
حالت modal یک کارت با گوشه‌های گرد و صفحه‌ی عقب‌رفته می‌سازد که push معمولی ندارد.
با رسیدن به iOS یا باید فقط آنجا modal را برگرداند، یا نسخه‌ی جدیدتر
`react-native-screens` را امتحان کرد که شاید این باگ را نداشته باشد.

### هر تغییر ناوبری باید یک بار در release تست شود

درس همان باگ بالا: بیلد debug و release در معماری جدید رفتار یکسانی ندارند و
دسته‌ای از باگ‌های mounting فقط در release دیده می‌شوند. قبل از هر انتشار،
حداقل یک بار همه‌ی صفحه‌ها را روی APK release باز کن.

### ~~حذف تراکنش فعلاً با لمس ردیف است~~ — ویرایش اضافه شد

لمس ردیف حالا [EditTransactionScreen](src/screens/EditTransactionScreen.tsx) را باز
می‌کند: همان فرم ثبت دستی، پرشده، به‌علاوه‌ی لینک «حذف این تراکنش» با تایید.
دیگر لمسِ ساده مستقیم سراغ یک عمل برگشت‌ناپذیر نمی‌رود.

آنچه باقی است:

- **بازگردانی (undo) ندارد.** حذف قطعی است؛ تنها راه برگشت، فایل پشتیبان است.
- **تاریخ قابل ویرایش نیست.** اگر پارسر تاریخ را اشتباه بخواند، کاربر نمی‌تواند
  اصلاحش کند. انتخابگر تاریخ شمسی لازم دارد.

### ~~تاریخ از متن پیامک استخراج نمی‌شود~~ ✅ انجام شد

`extractDate` در [smsParser.ts](src/services/smsParser.ts) سه شکل رایج را می‌خواند:
`1404/06/14-21:12`، `06/18-08:32` (سال از تاریخ جاری) و فقط ساعت. اگر ماه/روز
تاریخی در آینده بسازد، سال قبل در نظر گرفته می‌شود — پیامک اسفند که در فروردین
وارد می‌شود.

هنوز فرض بر این است که تاریخ شمسی است؛ پیامک با تاریخ میلادی پشتیبانی نمی‌شود.

### کرشِ گزارش‌شده در اولین ناوبری — علت ساختاری برداشته شد، ولی بازتولید نشد

روی گوشی یک‌بار با زدن «ثبت دستی» بلافاصله بعد از باز شدن اپ کرش گزارش شد و
بعدش دیگر تکرار نشد. روی شبیه‌ساز بازتولید نشد، پس **تایید نشده که رفع شده**.

چیزی که پیدا و برداشته شد: تا وقتی معرفی اولیه دیده نشده بود اصلاً
`NavigationContainer` رندر نمی‌شد و بعد از تمام شدن معرفی تازه ساخته می‌شد.
یعنی کاربر تازه چند لحظه بعد از mount شدن استک اولین صفحه را باز می‌کرد —
همان لحظه‌ای که react-native-screens قبلاً هم در آن کرش کرده بود.

حالا معرفی خودش یکی از صفحه‌های استک است و کانتینر از ثانیه‌ی اول یک‌بار
mount می‌شود و دیگر ساخته نمی‌شود. اگر باز هم روی گوشی دیدی، لاگ را با
`adb logcat -b crash` بگیر.

### خواندن خودکار پیامک — نکته‌های باقی‌مانده

[SmsReceiver.kt](android/app/src/main/java/com/polbin/SmsReceiver.kt) پیامک تازه را
می‌گیرد. اگر اپ جلوی چشم کاربر باشد متن را رویداد می‌کند و صفحه‌ی تایید باز می‌شود؛
وگرنه نوتیفیکیشنی می‌گذارد که با لمسش اپ با همان `ACTION_SEND` باز می‌شود — همان
مسیر «هم‌رسانی»، پس کد جاوااسکریپت تازه‌ای لازم نشد.

- **دریافت واقعی پیامک روی شبیه‌ساز تست نشده است.** ایمیج فعلی (API 36) پیامک
  تزریقی را اصلاً به اندروید نمی‌رساند: هم `adb emu sms send` و هم `adb emu sms pdu`
  جواب `OK` می‌دهند ولی بافر رادیو (`adb logcat -b radio`) هیچ پیامکی نشان نمی‌دهد.
  یعنی مشکل قبل از رسیدن به کد ماست. آنچه تست شد: ثبت شدن receiver در مانیفست،
  گرفتن هر دو مجوز، و ناپدید شدن کارت بعد از اجازه. آنچه تست نشد: خود `onReceive`.
  **اولین کاری که روی گوشی واقعی باید بکنی همین است** — یک پیامک بانکی واقعی
  بیاید و ببین صفحه‌ی تایید باز می‌شود یا نه.
- **کافه‌بازار باید مجوز پیامک را تایید کند.** `RECEIVE_SMS` مجوز حساسی است و
  بازارها معمولاً توضیح می‌خواهند. توضیح ما ساده است: پارس روی همان گوشی انجام
  می‌شود و متن پیامک هیچ‌جا ذخیره یا فرستاده نمی‌شود. اگر رد شد، مسیر «هم‌رسانی»
  دست‌نخورده باقی مانده و اپ بدون این مجوز هم کامل کار می‌کند.
- **پیامک‌های قدیمی وارد نمی‌شوند.** فقط پیامک‌های تازه گرفته می‌شوند. برای وارد
  کردن تاریخچه‌ی صندوق پیامک، مجوز `READ_SMS` جدا لازم است که مجوز سنگین‌تری
  است — عمداً فعلاً نگرفتیم.
- **فیلتر پیامک سه‌شرطی است، نه دوشرطی.** نسخه‌ی اول فقط کلیدواژه‌ی تراکنش و عدد
  می‌خواست و این پیامک اپراتور را بانکی حساب کرد:

  ```
  با خريد هر شارژ 100,000 توماني ... يک امتياز در قرعه کشي
  ```

  حالا [SmsFilter](android/app/src/main/java/com/polbin/SmsFilter.kt) نشانه‌ی حساب
  هم می‌خواهد (مانده/موجودی/حساب/سپرده/کارت/شبا) و لیست سیاه تبلیغات دارد.
  «تماس» و «شارژ» عمداً در لیست سیاه **نیستند**: پیامک واقعی بانک ممکن است بگوید
  «در صورت مغایرت تماس بگیرید» یا خرید شارژ را ثبت کند.
- **منطق فیلتر از Receiver جدا شد تا تست‌پذیر باشد.** `SmsFilter` هیچ وابستگی به
  اندروید ندارد و ۱۱ تست JVM دارد:

  ```powershell
  .\android\gradlew.bat -p android :app:testDebugUnitTest
  ```

  نبودِ همین تست بود که گذاشت آن مثبت کاذب تا گوشی کاربر برود.
- **پیامک تکراری بررسی نمی‌شود.** اگر کاربر همان پیامک را دستی هم هم‌رسانی کند،
  دو صفحه‌ی تایید می‌بیند. تشخیص تکراری (مبلغ + زمان نزدیک) هنوز نداریم.

### بودجه‌ی ماهانه — چه هست و چه نیست

سقف خرج ماهانه در [BudgetContext](src/state/BudgetContext.tsx) نگه داشته می‌شود و
کارت [BudgetCard](src/components/BudgetCard.tsx) پیشرفت ماه جاری را نشان می‌دهد.
کارت همیشه ماه جاری را می‌سنجد، مستقل از بازه‌ای که در داشبورد انتخاب شده.

از نسخه‌ی ۲ قالب پشتیبان، بودجه هم داخل فایل ذخیره می‌شود؛ فایل‌های نسخه‌ی ۱
هنوز خوانده می‌شوند و بودجه‌شان خالی در نظر گرفته می‌شود.

آنچه هنوز نیست:

- **سقف به تفکیک دسته نداریم.** فقط یک عدد کلی. برای شروع عمدی بود — کاربر روز
  اول نمی‌داند ماهی چقدر خرج غذا می‌کند — ولی وقتی چند ماه داده جمع شد، سقف
  دسته‌ای معنا پیدا می‌کند.
- **هشدار فعال نداریم.** کارت رنگ عوض می‌کند ولی وقتی از سقف رد شدی نوتیفیکیشنی
  نمی‌آید. حالا که کانال نوتیفیکیشن برای پیامک ساخته شده، این کار سختی نیست.
- **بودجه تاریخچه ندارد.** یک عدد است که با عوض کردنش، سقف ماه‌های قبل هم عوض
  می‌شود. برای مقایسه‌ی «شهریور از سقفش رد شد یا نه» باید سقف هر ماه جدا بماند.
- **بازه‌ی «۳۰ روز» حذف شد.** جایش «این ماه» و «ماه قبل» آمد. اگر کسی واقعاً
  پنجره‌ی غلتان می‌خواست، برگرداندنش یک سطر است.

## ۴. بدهی فنی

این‌ها الان درد ندارند، ولی با رشد پروژه پیدا می‌شوند.

### رشته‌های فارسی hardcode هستند
هیچ لایه‌ی i18n وجود ندارد. برای تک‌زبانه اشکالی ندارد؛ اگر انگلیسی یا iOS خواستی،
همه باید بیرون کشیده شوند.

### هر افزودن تراکنش کل درخت را re-render می‌کند
`TransactionsContext` یک شیء واحد است. روی ۱۹ تراکنش نامحسوس است، روی ۲۰۰۰ تا
محسوس می‌شود. راه‌حل: تفکیک context یا `useSyncExternalStore`.

### صفحات هم fetch می‌کنند هم رندر
برای این حجم قابل قبول است. اولین جایی که با رشد باید بشکند، hookهای اختصاصی
مثل `useDashboardData` است.

### تست‌ها هنوز نازک‌اند

`__tests__/smsParser.test.ts` هشت حالت پارسر را پوشش می‌دهد (مبلغ در برابر شماره‌ی
حساب، مانده، تاریخ کامل و کوتاه، درآمد). `__tests__/App.test.tsx` فقط چک می‌کند
اپ رندر می‌شود.

جای بعدی که ارزش تست دارد `buildBreakdown` و `buildInsights` در
[analytics.ts](src/services/analytics.ts) است — توابع خالص با بیشترین منطق محاسباتی.

تست‌های پارسر موقع انتقالش به بک‌اند دو برابر ارزشمند می‌شوند چون همان‌ها را
می‌شود آنجا هم اجرا کرد.

---

## ۵. یادداشت‌های رفتاری

چیزهایی که باگ نیستند ولی باید بدانیم:

- **RTL بعد از نصب اول یک ری‌استارت کامل می‌خواهد.** `I18nManager.forceRTL(true)` در
  [index.js](index.js) یک فلگ نیتیو ست می‌کند که تا بسته و باز شدن اپ اعمال نمی‌شود.
- **دکمه‌ی «شبیه‌سازی پیامک بانکی» حذف شد.** برای ساختن داده‌ی تست روی شبیه‌ساز، یا دستی
  ثبت کن یا intent هم‌رسانی بفرست — دستورش در [RUN.md](RUN.md) هست.
- **`fakeSmsInbox.ts` عمداً مانده** با اینکه هیچ‌جا import نمی‌شود. شش پیامک نمونه‌ی واقعی
  بانک‌های ایرانی است که برای تست پارسر ارزش دارد و بازسازی‌اش وقت‌گیر است.
- **هر کد ۴ رقمی برای ورود پذیرفته می‌شود** — عمدی، تا وقتی OTP واقعی نیامده.
