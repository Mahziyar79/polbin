# کارهای باقی‌مانده — پول‌بین

هر چیزی که در طول توسعه آگاهانه به تعویق انداختیم، با دلیلش. راهنمای اجرا و
عیب‌یابی محیط در [RUN.md](RUN.md) است، اینجا فقط کارهای کد و محصول.

آخرین به‌روزرسانی: ۲۰ شهریور ۱۴۰۵

---

## ۱. مسدودکننده‌های انتشار

این‌ها باید **قبل از** فرستادن به کافه‌بازار حل شوند.

### ~~معماری بیلد را برگردان~~ ✅ انجام شد
`reactNativeArchitectures` در [android/gradle.properties](android/gradle.properties) دوباره
روی هر چهار معماری است، پس APK روی گوشی‌های واقعی ARM نصب می‌شود.

### ~~کلید امضای release بساز~~ ✅ انجام شد

[android/app/build.gradle](android/app/build.gradle) کلید را از چهار مقدار
`POLBIN_*` در `~/.gradle/gradle.properties` می‌خواند — بیرون از مخزن گیت. اگر
تنظیم نشده باشند بیلد نمی‌شکند ولی هشدار زرد چاپ می‌کند و به کلید debug
برمی‌گردد، تا کسی ندانسته نسخه‌ی غیرقابل‌انتشار نفرستد.

اثر انگشت کلید (برای بررسی هر بیلد آینده):

```
SHA-256: 1f6fa7547e2af531fed0fd7fdd93b95fda5a592b1a45501df317fafb05a63f5e
CN=Mahziyar Gilanpour, C=IR
```

بررسی امضای یک APK:

```powershell
& "E:\android_studio\Android\Sdk\build-tools\37.0.0\apksigner.bat" verify --print-certs "android\app\build\outputs\apk\release\app-arm64-v8a-release.apk"
```

**فایل `polbin-release.keystore` و رمزش برگشت‌ناپذیرند.** گم شدنشان یعنی اپ
منتشرشده هرگز آپدیت نمی‌شود و باید با شناسه‌ی جدید از اول منتشر شود. یک نسخه‌ی
پشتیبان جای دومی لازم است.

### احراز هویت واقعی
صفحه‌ی ورود جعلی حذف شد و جایش [ProfileContext](src/state/ProfileContext.tsx) نشست که
فقط یک نام محلی نگه می‌دارد. تا وقتی همه‌چیز روی گوشی است این درست‌ترین حالت است، ولی
با آمدن سرور باید ورود واقعی (پیامک + توکن) برگردد و آن‌وقت:

- توکن **نباید** در MMKV رمزنگاری‌نشده بنشیند. `react-native-keychain` از Keystore
  خود اندروید استفاده می‌کند و برای اپ مالی حداقلِ لازم است.
- `user_id` همیشه باید از توکن سمت سرور بیرون کشیده شود، نه از بدنه‌ی درخواست.

سرور در `polbin_server` نوشته و تست شده ولی عمداً وصل نشده است.

### حجم APK — چه شد و چه چیزی باقی است

APK اول ۶۹ مگابایت بود. با دو تغییر در
[android/app/build.gradle](android/app/build.gradle) به **۲۲ مگابایت** رسید:

- **تفکیک بر اساس معماری** (`splits.abi`) — قبلاً یک فایل هر چهار معماری را با خود
  می‌برد در حالی که هر گوشی فقط یکی را اجرا می‌کند. ۵۸ مگابایت کتابخانه‌ی نیتیو
  بود که سه‌چهارمش روی هر دستگاه بی‌مصرف می‌ماند.
- **روشن کردن R8** (`enableProguardInReleaseBuilds = true`) — `classes.dex` را
  کوچک می‌کند. تست شد: ماژول نیتیو، پشتیبان، بازگردانی و گزارش PDF همه سالم‌اند.

کف حجمی که باقی مانده تقریباً تمامش موتور است، نه کد ما:

```
libreactnative.so   6.9MB
libhermesvm.so      2.5MB
libc++_shared.so    1.3MB
سایر .so            ~4MB
classes.dex         ~5MB
کد و فونت خودمان    ~3MB   ← بسته‌ی JS ۱.۴ + وزیرمتن ۰.۵ + منابع
```

پایین‌تر از این با ری‌اکت‌نیتیو ممکن نیست. اگر روزی حجم بحرانی شد، تنها گزینه‌ی
واقعی بازنویسی نیتیو (Kotlin) است که برای MVP توجیه ندارد.

اگر کافه‌بازار آپلود چند APK را قبول کند، هر چهار فایل را بده تا هر کاربر
سبک‌ترین نسخه را بگیرد؛ وگرنه فقط `arm64-v8a` را بفرست که اکثریت مطلق
گوشی‌های امروزی است.

### پاک‌سازی مانیفست
مجوزهای اضافه‌ی `AndroidManifest.xml` را حذف کن و روی اندروید ۸ به بالا تست کن
(`minSdkVersion` روی ۲۴ است).

### تست روی گوشی واقعی
تمام تست‌های تا امروز روی شبیه‌ساز `x86_64` بوده. اپ **هرگز روی یک گوشی ARM اجرا نشده**.
برای اپ فارسی ریسک واقعی دارد: رندر فونت، RTL، و رفتار منوی هم‌رسانی روی پوسته‌های
سازنده‌ها (شیائومی، سامسونگ) با اندروید خام فرق می‌کند.

---

## ۲. قابلیت‌های اصلی که هنوز ساخته نشده‌اند

### ~~Share Intent اندروید~~ ✅ انجام شد
هر دو مسیر پیاده و تست شدند: اجرای سرد (از `getLaunchOptions` به‌عنوان initialProps) و
اپِ باز (`onNewIntent` → رویداد `polbin.sharedText`). کد در
[MainActivity.kt](android/app/src/main/java/com/polbin/MainActivity.kt) و
[shareIntent.ts](src/services/shareIntent.ts).

دو کاستی باقی‌مانده:

- **اگر پروسه زنده ولی ReactContext نابود شده باشد**، `onNewIntent` جایی برای فرستادن
  رویداد ندارد و متن بی‌صدا از دست می‌رود. حالت نادری است (اندروید معمولاً کل پروسه را
  می‌کشد)، ولی راه درست ذخیره‌ی متن در MMKV و خواندنش موقع بالا آمدن context است.
- **فقط `ACTION_SEND` با `text/plain`** پشتیبانی می‌شود. `ACTION_SEND_MULTIPLE`
  (هم‌رسانی چند پیامک با هم) نادیده گرفته می‌شود.

### بک‌اند — ساخته شد ولی وصل نیست
سرور در `polbin_server/` نوشته و تست شده است: احراز هویت با کد پیامکی، توکن JWT،
جدول‌های PostgreSQL و مسیرهای تراکنش و دسته‌بندی با جداسازی بر اساس `user_id`.
۲۲ تست سرتاسری پاس می‌شود، از جمله اینکه کاربری نتواند داده‌ی کاربر دیگر را بخواند یا پاک کند.

**عمداً به اپ وصل نشده.** برای MVP تصمیم گرفتیم همه‌چیز روی دستگاه بماند: بک‌اند برای
کاربر روز اول ارزشی ندارد، ورود اجباری بزرگ‌ترین نقطه‌ی ریزش است، هزینه‌ی هاست و پیامک
دارد، و داستان «پیامک از گوشی خارج نمی‌شود» را از بین می‌برد.

سیگنال‌هایی که یعنی وقتش رسیده: کاربرها بپرسند «گوشی‌ام را عوض کردم، داده‌هایم کجاست؟»،
یا قابلیتی بخواهی که ذاتاً سرور می‌خواهد.

وقتی وصلش کردیم، سرور باید **منبع حقیقت نباشد** — معماری local-first بماند و سرور فقط
همگام‌سازی و پشتیبان باشد، تا اپ آفلاین کامل کار کند.

مسیر وصل کردن، جایگزینی بدنه‌ی این پنج تابع است:

| تابع | endpoint |
|---|---|
| `requestOtp` | `POST /auth/otp/request` |
| `verifyOtp` | `POST /auth/otp/verify` |
| `parseSmsRemote` | `POST /sms/parse` |
| `fetchTransactions` | `GET /transactions` |
| `createTransaction` | `POST /transactions` |

- [src/services/smsParser.ts](src/services/smsParser.ts) را به Node منتقل کن — تابع خالص
  است و هیچ وابستگی به ری‌اکت‌نیتیو ندارد.
- OTP واقعی از یک پنل پیامکی ایرانی.

### الگوی کش را عوض کن
[src/state/TransactionsContext.tsx](src/state/TransactionsContext.tsx) الان **یک بار** از
سرور seed می‌گیرد و بعد همیشه از کش می‌خواند. این عمدی بود تا داده‌ی نمونه تراکنش‌های
دستی کاربر را پاک نکند.

با بک‌اند واقعی باید بشود: «کش را فوری نشان بده، هم‌زمان از سرور به‌روز کن».

---

## ۳. باگ‌ها و کاستی‌های شناخته‌شده

### کارت‌به‌کارت شماره‌ی کارت اشتباه می‌گیرد
پارسر با `/\*{2,}\s*(\d{4})/` اولین شماره‌ی کارت را برمی‌دارد که در پیامک انتقال وجه
کارت **مقصد** است نه مبدأ. باید با کلیدواژه‌های «به:» و «از:» تفکیک شود.

### دسته‌های پیش‌فرض حذف نمی‌شوند
تصمیم عمدی: `MERCHANT_RULES` در پارسر مستقیم به شناسه‌های `food`، `transport` و
بقیه اشاره می‌کند. اگر کاربر بخواهد دسته‌ی پیش‌فرضی را نبیند، راه درست **مخفی کردن**
است نه حذف — هنوز پیاده نشده.

### مهاجرت کلیدهای ذخیره‌سازی
کلیدهای MMKV نسخه‌دارند (`transactions.v1`) که جلوی کرش با داده‌ی ناسازگار را می‌گیرد،
ولی منطق مهاجرت فقط برای یک مورد نوشته شده.

**نمونه‌ی واقعی که پیش آمد:** با افزودن درآمد، هر دسته فیلد `kind` گرفت. دسته‌های
دلخواهی که کاربر قبلاً ساخته بود این فیلد را نداشتند و `categoriesOfKind` بی‌صدا
حذفشان می‌کرد. مهاجرت در [CategoriesContext.tsx](src/state/CategoriesContext.tsx)
اضافه شد که مقدار `debit` را برایشان می‌گذارد.

هر تغییر ساختاری بعدی همین مراقبت را لازم دارد.

### MMKV برای مقیاس بزرگ ساخته نشده
کل آرایه‌ی تراکنش‌ها زیر **یک کلید** به‌صورت JSON ذخیره می‌شود، پس هر ثبت تراکنش
تمام لیست را دوباره سریالایز و کل فایل را بازنویسی می‌کند. روی ۲۰ تراکنش نامحسوس
است، روی چند هزار تا محسوس می‌شود.

دو نشانه که وقت مهاجرت به SQLite رسیده: کاربران چند صد تراکنش جمع کنند، یا بخواهی
جست‌وجو و گزارش ماه‌به‌ماه اضافه کنی که با آرایه‌ی JSON یعنی خواندن کل داده در حافظه.

### ~~هیچ پشتیبان یا خروجی‌ای وجود ندارد~~ — انجام شد

صفحه‌ی «پشتیبان و خروجی» ([src/screens/BackupScreen.tsx](src/screens/BackupScreen.tsx))
با ماژول نیتیو [PolbinFilesModule.kt](android/app/src/main/java/com/polbin/PolbinFilesModule.kt)
سه کار می‌کند: خروجی JSON، بازگردانی از فایل، و گزارش PDF از راه پنجره‌ی چاپ اندروید.

آنچه هنوز باقی است:

- **گزارش PDF بازه‌ی زمانی ندارد** — همیشه همه‌ی تراکنش‌ها را چاپ می‌کند. وقتی کاربر
  دو سال داده داشته باشد، گزارش «فروردین تا خرداد» لازم می‌شود.
- **بازگردانی جایگزین می‌کند، ادغام نمی‌کند** — عمدی است تا تراکنش تکراری ساخته نشود،
  ولی کاربری که روی دو گوشی داده دارد راهی برای یکی کردنشان ندارد.
- **فونت گزارش، فونت سیستم است** — وزیرمتن داخل WebView در دسترس نیست. برای یکدست
  شدن باید فونت را base64 در همان HTML جا داد؛ حجم PDF بیشتر می‌شود.
- **پشتیبان خودکار نیست** — کاربر باید یادش باشد دکمه را بزند. یادآور دوره‌ای یا
  ذخیره‌ی خودکار در پوشه‌ی دانلود قدم بعدی است.

---

### `presentation: 'modal'` برداشته شد — روی iOS باید بازبینی شود

اولین بیلد release نشان داد هر پنج صفحه‌ی فرعی با `presentation: 'modal'` اپ را می‌بندند:

```
IllegalStateException: addViewAt: failed to insert view [456] into parent [200] at index 2
  at com.swmansion.rnscreens.ScreenContainer.addScreen(ScreenContainer.kt:107)
Caused by: IndexOutOfBoundsException: Index: 2, Size: 1
```

آن حالت در `react-native-screens` یک ScreenStack جداگانه می‌سازد و با Fabric شمارش
فرزندهای نیتیو با درخت React هم‌خوان نمی‌ماند. **در بیلد debug هیچ‌وقت رخ نمی‌دهد** —
تمام صفحه‌ها ماه‌ها در debug باز می‌شدند و مشکلی نداشتند.

روی اندروید `slide_from_bottom` همان ظاهر را می‌دهد، پس چیزی از دست نرفت. ولی روی iOS
حالت modal یک کارت با گوشه‌های گرد و صفحه‌ی عقب‌رفته می‌سازد که push معمولی ندارد.
با رسیدن به iOS یا باید فقط آنجا modal را برگرداند، یا نسخه‌ی جدیدتر
`react-native-screens` را امتحان کرد که شاید این باگ را نداشته باشد.

### هر تغییر ناوبری باید یک بار در release تست شود

درس همان باگ بالا: بیلد debug و release در معماری جدید رفتار یکسانی ندارند و
دسته‌ای از باگ‌های mounting فقط در release دیده می‌شوند. قبل از هر انتشار،
حداقل یک بار همه‌ی صفحه‌ها را روی APK release باز کن.

### ~~حذف تراکنش فعلاً با لمس ردیف است~~ — ویرایش اضافه شد

لمس ردیف حالا [EditTransactionScreen](src/screens/EditTransactionScreen.tsx) را باز
می‌کند: همان فرم ثبت دستی، پرشده، به‌علاوه‌ی لینک «حذف این تراکنش» با تایید.
دیگر لمسِ ساده مستقیم سراغ یک عمل برگشت‌ناپذیر نمی‌رود.

آنچه باقی است:

- **بازگردانی (undo) ندارد.** حذف قطعی است؛ تنها راه برگشت، فایل پشتیبان است.
- ~~**تاریخ قابل ویرایش نیست.**~~ ✅ انجام شد. فرم تراکنش (ثبت دستی، تایید پیامک،
  ویرایش) ردیف «تاریخ و ساعت» دارد که
  [JalaliDatePicker](src/components/JalaliDatePicker.tsx) را در حالت `withTime` باز
  می‌کند: روز از تقویم، ساعت و دقیقه با دو فیلد عددی، و «تایید». پیش‌فرض ثبت دستی
  «همین لحظه» است، در تایید پیامک تاریخِ خودِ پیامک، و در ویرایش تاریخ ذخیره‌شده.
  شیت با باز شدن کیبورد بالا می‌آید (همان `useKeyboardHeight`)؛ در آن حالت بالای
  تقویم زیر نوار وضعیت می‌رود، که قابل قبول است چون کاربر در آن لحظه فقط با ساعت
  کار دارد.

### ~~تاریخ از متن پیامک استخراج نمی‌شود~~ ✅ انجام شد

`extractDate` در [smsParser.ts](src/services/smsParser.ts) سه شکل رایج را می‌خواند:
`1404/06/14-21:12`، `06/18-08:32` (سال از تاریخ جاری) و فقط ساعت. اگر ماه/روز
تاریخی در آینده بسازد، سال قبل در نظر گرفته می‌شود — پیامک اسفند که در فروردین
وارد می‌شود.

هنوز فرض بر این است که تاریخ شمسی است؛ پیامک با تاریخ میلادی پشتیبانی نمی‌شود.

### کرشِ گزارش‌شده در اولین ناوبری — علت ساختاری برداشته شد، ولی بازتولید نشد

روی گوشی یک‌بار با زدن «ثبت دستی» بلافاصله بعد از باز شدن اپ کرش گزارش شد و
بعدش دیگر تکرار نشد. روی شبیه‌ساز بازتولید نشد، پس **تایید نشده که رفع شده**.

چیزی که پیدا و برداشته شد: تا وقتی معرفی اولیه دیده نشده بود اصلاً
`NavigationContainer` رندر نمی‌شد و بعد از تمام شدن معرفی تازه ساخته می‌شد.
یعنی کاربر تازه چند لحظه بعد از mount شدن استک اولین صفحه را باز می‌کرد —
همان لحظه‌ای که react-native-screens قبلاً هم در آن کرش کرده بود.

حالا معرفی خودش یکی از صفحه‌های استک است و کانتینر از ثانیه‌ی اول یک‌بار
mount می‌شود و دیگر ساخته نمی‌شود. اگر باز هم روی گوشی دیدی، لاگ را با
`adb logcat -b crash` بگیر.

### خواندن خودکار پیامک — نکته‌های باقی‌مانده

[SmsReceiver.kt](android/app/src/main/java/com/polbin/SmsReceiver.kt) پیامک تازه را
می‌گیرد. اگر اپ جلوی چشم کاربر باشد متن را رویداد می‌کند و صفحه‌ی تایید باز می‌شود؛
وگرنه نوتیفیکیشنی می‌گذارد که با لمسش اپ با همان `ACTION_SEND` باز می‌شود — همان
مسیر «هم‌رسانی»، پس کد جاوااسکریپت تازه‌ای لازم نشد.

- **دریافت واقعی پیامک روی شبیه‌ساز تست نشده است.** ایمیج فعلی (API 36) پیامک
  تزریقی را اصلاً به اندروید نمی‌رساند: هم `adb emu sms send` و هم `adb emu sms pdu`
  جواب `OK` می‌دهند ولی بافر رادیو (`adb logcat -b radio`) هیچ پیامکی نشان نمی‌دهد.
  یعنی مشکل قبل از رسیدن به کد ماست. آنچه تست شد: ثبت شدن receiver در مانیفست،
  گرفتن هر دو مجوز، و ناپدید شدن کارت بعد از اجازه. آنچه تست نشد: خود `onReceive`.
  **اولین کاری که روی گوشی واقعی باید بکنی همین است** — یک پیامک بانکی واقعی
  بیاید و ببین صفحه‌ی تایید باز می‌شود یا نه.
- **کافه‌بازار باید مجوز پیامک را تایید کند.** `RECEIVE_SMS` مجوز حساسی است و
  بازارها معمولاً توضیح می‌خواهند. توضیح ما ساده است: پارس روی همان گوشی انجام
  می‌شود و متن پیامک هیچ‌جا ذخیره یا فرستاده نمی‌شود. اگر رد شد، مسیر «هم‌رسانی»
  دست‌نخورده باقی مانده و اپ بدون این مجوز هم کامل کار می‌کند.
- **پیامک‌های قدیمی وارد نمی‌شوند.** فقط پیامک‌های تازه گرفته می‌شوند. برای وارد
  کردن تاریخچه‌ی صندوق پیامک، مجوز `READ_SMS` جدا لازم است که مجوز سنگین‌تری
  است — عمداً فعلاً نگرفتیم.
- **فیلتر پیامک سه‌شرطی است، نه دوشرطی.** نسخه‌ی اول فقط کلیدواژه‌ی تراکنش و عدد
  می‌خواست و این پیامک اپراتور را بانکی حساب کرد:

  ```
  با خريد هر شارژ 100,000 توماني ... يک امتياز در قرعه کشي
  ```

  حالا [SmsFilter](android/app/src/main/java/com/polbin/SmsFilter.kt) نشانه‌ی حساب
  هم می‌خواهد (مانده/موجودی/حساب/سپرده/کارت/شبا) و لیست سیاه تبلیغات دارد.
  «تماس» و «شارژ» عمداً در لیست سیاه **نیستند**: پیامک واقعی بانک ممکن است بگوید
  «در صورت مغایرت تماس بگیرید» یا خرید شارژ را ثبت کند.
- **منطق فیلتر از Receiver جدا شد تا تست‌پذیر باشد.** `SmsFilter` هیچ وابستگی به
  اندروید ندارد و ۱۱ تست JVM دارد:

  ```powershell
  .\android\gradlew.bat -p android :app:testDebugUnitTest
  ```

  نبودِ همین تست بود که گذاشت آن مثبت کاذب تا گوشی کاربر برود.
- ~~**پیامک تکراری بررسی نمی‌شود.**~~ ✅ انجام شد — بخش «تشخیص تراکنش تکراری» پایین‌تر.

### قفل اپ — رمز چهاررقمی و اثر انگشت

با آمدن موجودی کارت‌ها روی داشبورد، اپِ بدون قفل یعنی هر کسی گوشی را بردارد
مانده‌ی حساب‌ها را می‌بیند. [LockContext](src/state/LockContext.tsx) وضعیت را نگه
می‌دارد، [LockScreen](src/components/LockScreen.tsx) یک لایه‌ی مطلق روی ناوبری است
(نه `Modal` و نه صفحه‌ی استک — ناوبری زیرش زنده می‌ماند تا پیامکی که با نوتیف
رسیده بعد از باز شدن سر جایش باشد)، و
[LockSettingsScreen](src/screens/LockSettingsScreen.tsx) از منو تنظیمش می‌کند.

- **رمز هیچ‌جا ذخیره نمی‌شود**؛ PBKDF2 با نمک تصادفی در
  [PinHasher.kt](android/app/src/main/java/com/polbin/PinHasher.kt) (تست JVM دارد).
  رمز چهاررقمی ده هزار حالت دارد و هیچ هشی آن را در برابر فایل‌خوانی روی گوشی روت
  «امن» نمی‌کند؛ کاری که هش می‌کند این است که خودِ رمز جایی نوشته نشود.
- **بعد از پنج رمز غلط، فاصله‌ی اجباری** که هر بار دو برابر می‌شود تا پنج دقیقه
  ([lockPolicy.ts](src/services/lockPolicy.ts)).
- **مهلت بیست ثانیه بعد از پس‌زمینه** — چون خودِ اپ کاربر را بیرون می‌فرستد (منوی
  هم‌رسانی، انتخاب فایل، چاپ). بدون آن هر خروجی اکسل سه بار رمز می‌خواست.
- **اثر انگشت با `BiometricPrompt`** — روی شبیه‌ساز با اثر انگشت ثبت‌شده‌ی مجازی تست
  شد (`adb emu finger touch 1`). تنها وابستگی نیتیو تازه: `androidx.biometric`.
- **فراموشی رمز راه بازیابی ندارد** — سروری نیست. راهش پاک کردن داده‌ی اپ از تنظیمات
  اندروید و بازگردانی پشتیبان است؛ صفحه‌ی قفل همین را می‌گوید.
- **پیش‌نمایش «برنامه‌های اخیر» اندروید هنوز محتوای اپ را نشان می‌دهد.** `FLAG_SECURE`
  این را می‌بندد ولی اسکرین‌شات گرفتن کاربر از گزارش خودش را هم می‌بندد؛ فعلاً نگذاشتم.
- **صفحه‌کلید عددی ۱ ۲ ۳ از چپ به راست است** حتی در اپ راست‌به‌چپ — مثل شماره‌گیر
  گوشی. ردیف‌ها در کد برعکس نوشته شده‌اند تا RTL درستشان کند.

**آینه‌ی Google Maven.** `dl.google.com` از این شبکه گاهی برای همه‌چیز ۴۰۴ می‌دهد.
تا وقتی همه‌ی وابستگی‌ها در کش بود دیده نمی‌شد؛ `androidx.biometric` اولین وابستگی
تازه بود و بیلد را شکست. `android/build.gradle` حالا `maven.myket.ir` و آینه‌ی
Aliyun را بعد از `google()` دارد.

### موجودی هر کارت — عدد بانک، نه محاسبه‌ی ما

پارسر «مانده/موجودی» را که قبلاً فقط دور می‌انداخت حالا در `Transaction.balance`
(به تومان) و چهار رقم آخر حساب را در `accountLast4` نگه می‌دارد.
[balances.ts](src/services/balances.ts) برای هر حساب آخرین مانده را برمی‌دارد و
[BalancesCard](src/components/BalancesCard.tsx) بالای داشبورد نشانش می‌دهد.

- **هیچ حسابی روی خرج و درآمد نمی‌شود.** تراکنش دستی و پیامکی که نرسیده جمع را
  غلط می‌کنند؛ عددِ خودِ بانک حرف آخر است. کنار هر ردیف زمان پیامک هست و بعد از یک
  هفته کم‌رنگ می‌شود.
- **کارت و حساب عمداً ادغام نمی‌شوند.** از پیامک درنمی‌آید کدام کارت به کدام حساب
  وصل است؛ دو ردیف بهتر از یک ردیف با مانده‌ی اشتباه است. اگر کاربرها گله کردند، راهش
  یک صفحه‌ی «حساب‌ها» است که کاربر خودش کارت را به حساب وصل کند.
- تراکنش‌های ثبت‌شده قبل از این نسخه مانده ندارند؛ با اولین پیامک بعدی درست می‌شود.
- ستون «مانده بعد از تراکنش» به خروجی اکسل هم اضافه شد.
- **بانکی که کارت یا حساب نمی‌گوید (بلوبانک) فقط با نام بانک یک ردیف می‌شود.** بلو
  نئوبانک است و هر کاربر یک حساب اصلی دارد، پس معمولاً درست است. اگر کسی چند حساب
  در یک بانکِ بی‌شماره داشته باشد، همه روی هم می‌افتند — تا وقتی کسی گله نکرده، عمداً
  ساده مانده.

پیامک واقعی بلوبانک سه چیز را نشان داد که هیچ‌کدام در نمونه‌های قبلی نبود و حالا
تست دارند:

```
بلو
برداشت پول
مهزیار عزیز، 20,000,000 ریال از حساب شما پرید.
موجودی: 528,926,412 ریال
۸:۳۴
۱۴۰۵.۰۶.۲۰
```

- بانک خودش را فقط «بلو» می‌نامد نه «بلوبانک» → `aliases` در
  [banks.ts](src/data/banks.ts). قبلش این پیامک «بانک نامشخص» می‌شد.
- تاریخ با نقطه (`۱۴۰۵.۰۶.۲۰`) و ساعت در خط جدا → پارسر تاریخ فقط `/` را می‌شناخت و
  این پیامک همیشه «امروز» ثبت می‌شد؛ برای پیامکی که یک روز بعد هم‌رسانی شود غلط بود.
- مانده‌ی ریالی فرد، تومان اعشاری می‌داد (۵۲٬۸۹۲٬۶۴۱٫۲) → گرد می‌شود.

### خروجی اکسل — xlsx واقعی، بدون کتابخانه

[xlsx.ts](src/services/xlsx.ts) فایل .xlsx را از صفر می‌سازد: zip بدون فشرده‌سازی
(«stored») + XMLهای SpreadsheetML با رشته‌های داخل‌خطی. کتابخانه‌های xlsx برای
ری‌اکت‌نیتیو یا یک مگابایت به بسته اضافه می‌کنند یا polyfill نیتیو می‌خواهند؛ این
نسخه کمتر از دویست خط است و با `zipfile` و `openpyxl` پایتون بی‌خطا و بی‌هشدار
باز شد (CRC همه‌ی بخش‌ها درست، شیت راست‌به‌چپ، ردیف تیتر ثابت، مبلغ عدد واقعی
با فرمت `#,##0`).

- **مبلغ عدد است نه متن** تا کاربر بتواند SUM بزند. تاریخ شمسی متن است (اکسل
  تاریخ شمسی ندارد)؛ برای مرتب‌سازی، ستون آخر تاریخ میلادی ISO است.
- **متن خام پیامک عمداً در خروجی نیست** — همان دلیل فایل پشتیبان.
- bridge ری‌اکت‌نیتیو فقط رشته می‌برد، پس بایت‌ها base64 می‌شوند و
  `saveAndShareBase64` در ماژول نیتیو بازشان می‌کند. base64 و UTF-8 و CRC32 همه
  دستی‌اند چون Hermes `Buffer` ندارد و `TextEncoder`ش قابل اتکا نبود.
- **بزرگ‌ترین خطر پنهان:** spread کردن آرایه‌ی بایت داخل `push` یا فراخوانی تابع، با
  چند صد کیلوبایت پشته‌ی Hermes را می‌ترکاند. چسباندن تکه‌ها با `Uint8Array.set`
  انجام می‌شود، نه spread. اگر کسی این را «ساده‌تر» کرد، روی چند هزار تراکنش
  می‌شکند نه روی ده تا.
- برای بازتولید فایل نمونه و بررسی با ابزار بیرونی: یک تست موقت که خروجی
  `buildTransactionsXlsx` را با `fs.writeFileSync` روی دیسک بنویسد، بعد
  `python -c "import openpyxl; openpyxl.load_workbook('sample.xlsx')"`.

### تشخیص تراکنش تکراری — هشدار می‌دهد، حذف نمی‌کند

[duplicates.ts](src/services/duplicates.ts) قبل از ثبت، تراکنش تازه را با ثبت‌شده‌ها
می‌سنجد و [DuplicateCard](src/components/DuplicateCard.tsx) بالای فرم تایید نتیجه را
نشان می‌دهد. دو قاعده، از قوی به ضعیف:

- **متن خام یکسان** بعد از نرمال‌سازی و حذف فاصله‌ها — قطعاً همان پیامک است، حتی اگر
  کاربر مبلغ را قبل از ثبت دستی عوض کرده باشد. لحن کارت قاطع است: «این پیامک را قبلاً
  ثبت کرده‌ای».
- **مبلغ + نوع + زمان نزدیک**، به شرط اینکه بانک‌ها (اگر هر دو معلوم‌اند) یکی باشند. بازه
  ۱۰ دقیقه است وقتی هر دو از پیامک آمده‌اند (بانک + درگاه)، و «همان روز» وقتی یکی
  دستی ثبت شده — چون زمان ثبت دستی لحظه‌ی ثبت است نه لحظه‌ی خرید. لحن کارت نرم است:
  «شبیه تراکنشی است که قبلاً ثبت شده».

تصمیم‌های عمدی:

- **هیچ‌چیز خودکار حذف نمی‌شود.** دو قهوه‌ی پنجاه‌هزاری با دو دقیقه فاصله واقعی است.
- **کارت یک بار، لحظه‌ی پارس، حساب می‌شود** نه با هر ویرایش کاربر؛ وگرنه وسط تایپ
  ظاهر و ناپدید می‌شد.
- **فرم ثبت دستی فقط یک خط نرم دارد، بدون دکمه، و فقط در برابر تراکنش‌های پیامکی.**
  حالت واقعی «پیامک خودکار ثبت شده و کاربر یادش رفته» را می‌گیرد، بدون اینکه دو خرج
  دستیِ هم‌مبلغ در یک روز هشدار بگیرند.
- **سمت نیتیو دست نخورد.** `SmsReceiver` همان است که بود؛ تشخیص کاملاً در JS و بعد از
  پارس است، پس رسیدن پیامک از این تغییر تاثیر نمی‌گیرد.

باقی‌مانده:

- فایل پشتیبان عمداً `rawSms` ندارد، پس بعد از بازگردانی فقط قاعده‌ی دوم کار می‌کند.
- گوشی دو سیم‌کارته یا پوسته‌ای که یک پیامک را دو بار به receiver می‌دهد، دو نوتیف
  می‌سازد؛ کارت جلوی ثبت دوم را می‌گیرد ولی نوتیف دوم را نه. اگر دیده شد، `SmsReceiver`
  می‌تواند هش چند پیامک آخر را نگه دارد.

**باگی که حین همین کار پیدا شد:** پارسر نام بانک را با `includes` ساده پیدا می‌کرد و
«دی» را داخل «موجودی»، «ملی» را داخل «عملیات» و «شهر» را داخل «شهریور» می‌گرفت —
تقریباً هر پیامکی که «موجودی» داشت «بانک دی» می‌شد. حالا نام بانک باید واژه‌ی مستقل
باشد (`findBankName`). `` جاوااسکریپت برای فارسی کار نمی‌کند، مرز دستی است.

### بودجه‌ی ماهانه — چه هست و چه نیست

سقف خرج ماهانه در [BudgetContext](src/state/BudgetContext.tsx) نگه داشته می‌شود و
کارت [BudgetCard](src/components/BudgetCard.tsx) پیشرفت ماه جاری را نشان می‌دهد.
کارت همیشه ماه جاری را می‌سنجد، مستقل از بازه‌ای که در داشبورد انتخاب شده.

از نسخه‌ی ۲ قالب پشتیبان، بودجه هم داخل فایل ذخیره می‌شود؛ فایل‌های نسخه‌ی ۱
هنوز خوانده می‌شوند و بودجه‌شان خالی در نظر گرفته می‌شود.

آنچه هنوز نیست:

- **سقف به تفکیک دسته نداریم.** فقط یک عدد کلی. برای شروع عمدی بود — کاربر روز
  اول نمی‌داند ماهی چقدر خرج غذا می‌کند — ولی وقتی چند ماه داده جمع شد، سقف
  دسته‌ای معنا پیدا می‌کند.
- **هشدار فعال نداریم.** کارت رنگ عوض می‌کند ولی وقتی از سقف رد شدی نوتیفیکیشنی
  نمی‌آید. حالا که کانال نوتیفیکیشن برای پیامک ساخته شده، این کار سختی نیست.
- **بودجه تاریخچه ندارد.** یک عدد است که با عوض کردنش، سقف ماه‌های قبل هم عوض
  می‌شود. برای مقایسه‌ی «شهریور از سقفش رد شد یا نه» باید سقف هر ماه جدا بماند.
- **بازه‌ی «۳۰ روز» حذف شد.** جایش «این ماه» و «ماه قبل» آمد. اگر کسی واقعاً
  پنجره‌ی غلتان می‌خواست، برگرداندنش یک سطر است.

### `adjustResize` در مانیفست بی‌اثر است — کیبورد را خودمان مدیریت می‌کنیم

`edgeToEdgeEnabled=true` است و از اندروید ۱۵ به بعد، اندروید برای اپ‌های
edge-to-edge پنجره را با `adjustResize` کوچک نمی‌کند. یعنی آن خط در
[AndroidManifest.xml](android/app/src/main/AndroidManifest.xml) عملاً کاری نمی‌کند
و کیبورد **روی** محتوا می‌افتد.

نتیجه‌اش یک باگ واقعی بود: دکمه‌ی پایین فرم زیر کیبورد گم می‌شد و کاربر روی
کیبورد لمس می‌کرد و فکر می‌کرد دکمه خراب است. هر چهار صفحه‌ی فرم و صفحه‌ی معرفی
درگیر بودند.

[useKeyboardHeight](src/hooks/useKeyboardHeight.ts) ارتفاع کیبورد را می‌دهد و
[FormFooter](src/components/FormFooter.tsx) به همان اندازه بالا می‌آید. چون فوتر
مشترک است، هر چهار صفحه با یک تغییر درست شدند.

**هر صفحه‌ی جدیدی که ورودی متنی و دکمه‌ی پایین دارد باید از `FormFooter` استفاده
کند**، وگرنه همین باگ برمی‌گردد.

#### نسخه‌ی اول این محاسبه غلط بود و روی گوشی دیده شد

کاربر گزارش داد دکمه‌ی «افزودن» گاهی کار نمی‌کند: «انگار یک لایه رویش است، باید
یک قسمت دیگرِ دکمه را بزنم». اندازه‌گیری روی دستگاه با
`adb shell uiautomator dump` و `dumpsys window` نشان داد چرا:

```
ارتفاع صفحه           1280
نوار ناوبری           [1232 .. 1280]   یعنی inset پایین = 48
لبه‌ی بالای کیبورد     682
کف فوتر (مرز لمس)     698   ← ۱۶ پیکسل زیر کیبورد
```

فرمول قبلی `endCoordinates.height - insets.bottom` بود، با این فرض که عددِ
گزارش‌شده‌ی کیبورد ارتفاع نوار ناوبری را در خود دارد. آن فرض درست نبود، پس ته
فوتر زیر کیبورد می‌ماند: **دیده می‌شد ولی لمسش به کیبورد می‌رسید، نه به دکمه.**

حالا حساب از روی **مختصات مطلقِ لبه‌ی بالای کیبورد** (`endCoordinates.screenY`)
انجام می‌شود که هیچ فرضی لازم ندارد، به‌علاوه‌ی یک فاصله‌ی اطمینان کوچک. بعد از
اصلاح، همان اندازه‌گیری کف فوتر را روی ۶۳۴ نشان می‌دهد — ۴۸ پیکسل بالای کیبورد.

`keyboardDidChangeFrame` هم اضافه شد. کیبورد فارسی وسط تایپ قدش عوض می‌شود (نوار
پیشنهاد کلمه، رفتن به صفحه‌ی اعداد یا ایموجی) و با گوش دادن فقط به
`keyboardDidShow` فوتر سر جای قبلی می‌ماند و کیبوردِ بلندتر رویش می‌افتد.

**درس روش کار:** این باگ ماه‌ها روی شبیه‌ساز پنهان ماند چون AVD با
`hw.keyboard=yes` از کیبورد سخت‌افزاری کامپیوتر استفاده می‌کند و **اصلاً کیبورد
نرم‌افزاری نشان نمی‌دهد**. برای تست هر چیزی که به کیبورد ربط دارد، در
`config.ini` مربوط به AVD مقدار `hw.keyboard=no` بگذار و شبیه‌ساز را دوباره بالا
بیاور. `adb shell settings put secure show_ime_with_hard_keyboard 1` کافی نیست.

### صفحه‌بندی تراکنش‌ها «نمایش بیشتر» است نه شماره‌ی صفحه

[TransactionsScreen](src/screens/TransactionsScreen.tsx) هر بار ۲۰ ردیف بیشتر
نشان می‌دهد. روی موبایل این از دکمه‌های قبلی/بعدی طبیعی‌تر است و هدف اصلی —
رندر نشدن یک‌باره‌ی کل لیست — را برآورده می‌کند.

اگر روزی لیست خیلی بزرگ شد، `FlatList` از قبل سر جایش است و می‌شود به‌جای دکمه،
`onEndReached` را وصل کرد.

### دو تله‌ی راست‌به‌چپ که یک‌بار گاز گرفتند

هر دو در کارت بودجه پیدا شدند و هر دو **فقط روی دستگاه** دیده می‌شوند، نه در
typecheck و نه در تست:

- **متن داخل ردیف باید `flex` داشته باشد.** تیتر «بودجه‌ی شهریور» داخل یک
  `flexDirection: 'row'` بدون عرض مشخص بود و اندروید دُمش را می‌انداخت — فقط
  «بودجه‌ی» رندر می‌شد. همان رشته بیرون از ردیف کامل نمایش داده می‌شد. با
  `flex: 1` درست شد.
- **جداکننده‌ی «·» را بین متن فارسی و عدد نگذار.** در چیدمان دوجهته سر جای غلطی
  می‌نشیند: «مانده · ۱۳ روز» شبیه «مانده ۱۳ ۰ روز» دیده می‌شد. جمله را طوری
  بنویس که بین کلمه و عدد فقط حرف فارسی باشد، یا به دو خط بشکن.

### وقتی یک کامپوننت دو حالت خیلی متفاوت دارد، دو کامپوننت بنویس

کارت بودجه اول یک تابع با `if` بود. با عوض شدن شرط، React همان viewهای نیتیو را
تطبیق می‌داد و `borderStyle: 'dashed'` حالت خالی روی حالت پرشده می‌ماند — چون
استایل جدید `borderStyle` نداشت، هیچ‌وقت ریست نمی‌شد. حالا `BudgetEmptyCard` و
`BudgetProgressCard` جدا هستند تا React مجبور به unmount شود.

### قسط‌ها — مرحله‌ی یک انجام شد، یادآوری هنوز نه

[InstallmentsScreen](src/screens/InstallmentsScreen.tsx) و
[AddInstallmentScreen](src/screens/AddInstallmentScreen.tsx) کار می‌کنند: هر برنامه
عنوان و مبلغ هر قسط و تعداد اقساط و تاریخ اولین سررسید دارد، و بقیه‌ی سررسیدها با
`addJalaliMonths` ماه‌به‌ماه ساخته می‌شوند. منطق خالص در
[installments.ts](src/services/installments.ts) با ۲۳ تست است، از جمله حالت‌های
لبه‌ی تقویم شمسی (اسفند به فروردین، ۳۱ام در ماه ۳۰روزه).

- **یادآوری خودکار نداریم** — مرحله‌ی دوم همین است و کار نیتیو می‌خواهد:
  `AlarmManager` برای زمان‌بندی روز قبلِ سررسید، به‌علاوه‌ی `POST_NOTIFICATIONS` و
  زنده ماندن زمان‌بندی بعد از ریبوت (`BOOT_COMPLETED`). فعلاً کاربر باید خودش
  صفحه را باز کند.
- **قسط پرداخت‌شده تراکنش نمی‌سازد.** علامت زدنش فقط وضعیت را عوض می‌کند و در
  «مجموع خرج» ماه دیده نمی‌شود. اگر بعداً تراکنش خودکار ساختیم، باید مراقب تکراری
  شدن با پیامک بانک بود.
- **مبلغ همه‌ی اقساط یکی فرض شده.** وام‌هایی که قسط آخرشان فرق دارد یا سود پلکانی
  دارند پشتیبانی نمی‌شوند.
- **بودجه فقط قسط‌های همین ماه را کنار می‌گذارد**، نه کل بدهی — عمدی است: عدد
  «۱۲۰ میلیون باقی‌مانده» فقط می‌ترساند و کاری از دست کاربر برنمی‌آید.
- از نسخه‌ی ۳ قالب پشتیبان، قسط‌ها هم داخل فایل می‌روند. فایل‌های نسخه‌ی ۱ و ۲
  هنوز خوانده می‌شوند و قسطشان خالی در نظر گرفته می‌شود.

### تله‌ی سومِ راست‌به‌چپ: جمله را با واژه‌ی فارسی شروع کن

جمله‌ی «سررسید فلان ۱۴ روز عقب افتاده» اول این‌طور نوشته شده بود:
`` `«${title}» ${n} روز عقب افتاده` ``. با عنوان فارسی درست بود، ولی با عنوان
لاتین («Vam») اولین نویسه‌ی جهت‌دارِ سطر لاتین می‌شد، اندروید کل پاراگراف را
چپ‌به‌راست می‌چید و خروجی می‌شد «روز عقب افتاده ۱۴ «Vam»».

گیومه نویسه‌ی خنثی است و جهت نمی‌دهد. **هر رشته‌ای که متن دلخواه کاربر را در خود
دارد باید با یک واژه‌ی فارسی شروع شود.**

## ۴. بدهی فنی

این‌ها الان درد ندارند، ولی با رشد پروژه پیدا می‌شوند.

### رشته‌های فارسی hardcode هستند
هیچ لایه‌ی i18n وجود ندارد. برای تک‌زبانه اشکالی ندارد؛ اگر انگلیسی یا iOS خواستی،
همه باید بیرون کشیده شوند.

### هر افزودن تراکنش کل درخت را re-render می‌کند
`TransactionsContext` یک شیء واحد است. روی ۱۹ تراکنش نامحسوس است، روی ۲۰۰۰ تا
محسوس می‌شود. راه‌حل: تفکیک context یا `useSyncExternalStore`.

### صفحات هم fetch می‌کنند هم رندر
برای این حجم قابل قبول است. اولین جایی که با رشد باید بشکند، hookهای اختصاصی
مثل `useDashboardData` است.

### تست‌ها منطق خالص را پوشش می‌دهند، رابط کاربری را نه

۷۶ تست جاوااسکریپت روی توابع خالص: پارسر پیامک، ماه شمسی، analytics، بانک‌ها،
قسط‌ها و پشتیبان. به‌علاوه‌ی ۱۱ تست JVM روی `SmsFilter`.

`__tests__/App.test.tsx` فقط چک می‌کند اپ رندر می‌شود. **هیچ تستی روی رابط کاربری
نداریم** و دقیقاً همان‌جاست که باگ‌های واقعی این پروژه پیدا شدند (چیدمان
راست‌به‌چپ، کیبورد روی دکمه، تطبیق شدن دو حالت یک کارت روی هم). این‌ها با
typecheck و تست واحد پیدا نمی‌شوند و تنها راهشان باز کردن صفحه روی دستگاه است.

تست‌های پارسر موقع انتقالش به بک‌اند دو برابر ارزشمند می‌شوند چون همان‌ها را
می‌شود آنجا هم اجرا کرد.

---

## ۵. یادداشت‌های رفتاری

چیزهایی که باگ نیستند ولی باید بدانیم:

- **RTL بعد از نصب اول یک ری‌استارت کامل می‌خواهد.** `I18nManager.forceRTL(true)` در
  [index.js](index.js) یک فلگ نیتیو ست می‌کند که تا بسته و باز شدن اپ اعمال نمی‌شود.
- **دکمه‌ی «شبیه‌سازی پیامک بانکی» حذف شد.** برای ساختن داده‌ی تست روی شبیه‌ساز، یا دستی
  ثبت کن یا intent هم‌رسانی بفرست — دستورش در [RUN.md](RUN.md) هست.
- **`fakeSmsInbox.ts` عمداً مانده** با اینکه هیچ‌جا import نمی‌شود. شش پیامک نمونه‌ی واقعی
  بانک‌های ایرانی است که برای تست پارسر ارزش دارد و بازسازی‌اش وقت‌گیر است.
- **هر کد ۴ رقمی برای ورود پذیرفته می‌شود** — عمدی، تا وقتی OTP واقعی نیامده.
